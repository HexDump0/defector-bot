// p3d (3bdmy2qwojynbk2rmsmc) mean=1.6606183608877227 stats={'battles': 751, 'losses': 273, 'wins': 10}
export default function bot({ history = [], memory }) { const m = (memory && typeof memory === 'object') ? memory : { d: 0, grudge: 0 }; m.d = Number.isFinite(m.d) ? m.d : 0; m.grudge = Number.isFinite(m.grudge) ? m.grudge : 0; if (history.length === 0) return ["C", m]; const opponentMove = history[history.length - 1].opponent; if (opponentMove === "D") { m.d++; if (m.d >= 2) m.grudge = 1; } else { m.d = 0; if (m.grudge) { m.grudge++; if (m.grudge >= 4) m.grudge = 0; } } if (m.grudge === 1) return ["D", m]; return [opponentMove === "C" ? "C" : "D", m]; }
